/**
   * Standard request methods that should be implemented
   * @readonly
   * @enum {string}
   */
  export const StandardMethods = {
    GET_TOKEN: 'starling:getToken',
    PING: 'system:ping',
    CAPABILITIES: 'system:capabilities'
  };